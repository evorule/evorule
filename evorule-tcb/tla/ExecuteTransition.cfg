(* ExecuteTransition.cfg — TLC 模型检测配置 *)
(* 设计文档：EVORULE_FORMAL_VERTIFICATION_PLAN.md §8.4.7, §8.6.2bis *)
(* 参数降级（策略 3）：N_MAX=2, D_MAX=2, D_DOM_MAX=2 *)
(* 控制流路径全覆盖：LengthCheck/Loop/BranchDepthCheck/DomainDepthCheck/
   IoReturn/ExtractResult 全部保留；深度强制 D_MAX=2 仍可验证 NestingTooDeep *)

SPECIFICATION Spec

INVARIANTS
TerminationInvariant
DeterminismInvariant
DepthEnforcementInvariant
IoEarlyReturnInvariant
LoopProgressInvariant

CONSTANTS
N_MAX = 2
D_MAX = 2
D_DOM_MAX = 2
InstrTypeSet = {"set", "push", "branch", "io_request"}
IoTypeSet = {"call_llm", "call_external", "query_db"}
