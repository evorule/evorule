# 变更审查表 (Change Request)

> **版本**: 2.0
> **用途**: evorule 核心模块变更治理的强制审查模板
> **规则**: 所有核心模块的修改必须附有此表，否则构建将被拒绝
> **核心原则**: evorule 仓是**机制层**，只允许机制层变更，策略层变更必须在应用层仓实现

---

## 1. 基本信息

| 字段 | 要求 | 说明 |
|------|------|------|
| **变更 ID** | 自动生成 | 格式: CR-YYYYMMDD-NNN |
| **变更标题** | 必填 | 一句话描述变更目的 |
| **提交人** | 必填 | 修改负责人姓名 |
| **提交日期** | 必填 | YYYY-MM-DD |
| **审查状态** | 必填 | `待审查` / `已批准` / `已拒绝` / `紧急通过` |

## 2. 变更层级判定（必填，核心门禁）

> **核心规则**: evorule 仓是**机制层**，只允许机制层变更。策略层变更**必须**在应用层仓（evorule-server/evorule-application 等）实现。

### 2.1 变更层级声明

**本次变更属于**: 
- [ ] **机制层 (Mechanism)** — 通用基础设施、执行引擎能力
- [ ] **策略层 (Strategy)** — 具体业务逻辑、场景化规则

> ⚠️ **警告**: 若声明为"策略层"，构建将被自动拒绝。策略层变更必须在应用层仓实施。

### 2.2 判定理由（必填）

> **要求**: 详细说明为什么本次变更是机制层变更，不是策略层变更

```
[在此填写判定理由，需明确说明]
- 是否包含特定业务语义？
- 是否只提供通用基础能力？
- 是否可被任何业务场景复用？
```

### 2.3 机制层判定标准（参考）

**✅ 机制层变更的特征**:
- [ ] 提供通用基础设施能力（如 JSON 解析、状态转换、哈希计算）
- [ ] 不包含任何特定业务语义（如"库存扣减"、"用户权限"）
- [ ] 可被任何业务场景无差别复用
- [ ] 代码中无硬编码的业务字面量（如特定领域的规则名、业务术语）
- [ ] 定义的是"怎么做"的通用方式，而非"做什么"的业务规则

**❌ 策略层变更的特征（禁止进入机制层）**:
- [ ] 包含特定业务场景逻辑（如医院、金融、法律等领域规则）
- [ ] 硬编码业务条件或业务规则名
- [ ] 实现的是控制流指令（conditional/while_loop/sequence）
- [ ] 包含具体 I/O Handler 实现
- [ ] 涉及业务编排或流程定义

---

## 3. 变更分类

### 变更类型（单选）
- [ ] **A**: 新增原语/机制
- [ ] **B**: 修复 Bug
- [ ] **C**: 性能优化
- [ ] **D**: 文档更新
- [ ] **E**: 代码重构（无功能变化）
- [ ] **F**: 其他

### 影响模块（多选）
- [ ] evorule-tcb (TCB 内核)
- [ ] evorule-reactor (ReActor 执行器)
- [ ] evorule-governance (治理层)
- [ ] evorule-cli (命令行接口)

---

## 3. 变更详情（必填）

### 3.1 变更理由
> **要求**: 说明为什么需要这次变更，解决什么问题，达到什么目标

```
[在此填写变更理由]
```

### 3.2 变更范围
> **要求**: 列出所有受影响的文件和代码区域

```
[在此列出受影响的文件路径]
```

### 3.3 破坏性分析
> **要求**:
> - 是否为破坏性变更？
> - 是否影响现有 API？
> - 是否影响现有数据格式？
> - 旧版本兼容性如何？

```
[在此填写破坏性分析]
```

### 3.4 影响评估
> **要求**:
> - 对系统稳定性的影响
> - 对现有功能的影响
> - 性能影响
> - 安全影响

```
[在此填写影响评估]
```

### 3.5 测试计划
> **要求**:
> - 需要运行的测试类型
> - 预期新增的测试用例
> - 回归测试覆盖范围

```
[在此填写测试计划]
```

### 3.6 回滚方案
> **要求**: 如果变更出现问题，如何快速回滚？

```
[在此填写回滚方案]
```

---

## 4. 审查清单（必须全部通过）

### 层级审查（新增，核心门禁）
- [ ] ✅ 变更层级声明为"机制层"（策略层变更在机制层禁止）
- [ ] ✅ 判定理由充分，明确说明变更是通用基础能力
- [ ] ✅ 代码中无策略层反模式（硬编码业务字面量、领域特定逻辑等）
- [ ] ✅ 若涉及新原语，确认是机制原语而非业务原语

### 技术审查
- [ ] ✅ 代码符合 TCB_SPEC.md 规范
- [ ] ✅ 无 unsafe/unwrap/expect 等危险构造
- [ ] ✅ 无 HashMap/HashSet 等非确定性容器
- [ ] ✅ 无系统时间、随机数、浮点等非确定性特性
- [ ] ✅ 无 I/O 操作（文件、网络、数据库）
- [ ] ✅ 保持确定性（deterministic）

### 架构审查
- [ ] ✅ 变更符合规则分类体系（rule_taxonomy.md）
- [ ] ✅ 规则类型明确（系统规则/宪法规则/元规则/用户规则）
- [ ] ✅ 修改权限符合设计要求
- [ ] ✅ 与现有模块职责边界清晰
- [ ] ✅ 符合"机制不染指控制流"原则

### 测试审查
- [ ] ✅ 单元测试覆盖率 >= 80%
- [ ] ✅ 集成测试通过
- [ ] ✅ 属性测试（proptest）通过
- [ ] ✅ 回归测试通过

### 文档审查（必填，参考 docs/CODE_DOC_MAP.md）

> **工具提示**: commit 前运行 `powershell -File scripts/pre-commit-doc-check.ps1` 可自动生成需检查的文档清单。

**通用文档**:
- [ ] ✅ 根 `CHANGELOG.md` 已更新（任何用户可见变更必填）
- [ ] ✅ `DOCS_INDEX.md` 已同步（新增/删除公开文档时必填）
- [ ] ✅ `GATE_REFERENCE.md` 已同步（任何 crate 的 build.rs 门禁变更时必填）

**按受影响 crate 检查（在本次变更涉及的 crate 前打勾）**:
- [ ] **evorule-tcb**: `TCB_SPEC.md` + `README.md` 已同步
- [ ] **evorule-reactor**: `REACTOR_SPEC.md` + `README.md` 已同步
- [ ] **evorule-governance**: `GOVERNANCE_SPEC.md` + `README.md` 已同步
- [ ] **evorule-cli**: `CLI_SPEC.md` + `README.md` + `CHANGELOG.md` 已同步

**特殊变更检查**:
- [ ] ✅ `core_eval.json` 变更 → 教程（docs/tutorial/02、03）+ 根 README 已同步
- [ ] ✅ 公开 API / 用户可见行为变更 → 对应教程（docs/tutorial/）已同步
- [ ] ✅ 新增/删除公开文档 → `DOCS_INDEX.md` + `docs/SUMMARY.md` 已同步
- [ ] ✅ Kani 验证变更 → 对应 verification/ 文档已同步
- [ ] ✅ 代码注释清晰，无遗留 TODO/FIXME

**文档同步确认**:
- [ ] ✅ 已运行 `scripts/pre-commit-doc-check.ps1` 并确认所有建议文档已处理
- [ ] ✅ 已查阅 `docs/CODE_DOC_MAP.md` 确认无遗漏

---

## 5. 紧急通道（特殊情况）

> **警告**: 紧急通道仅限生产环境严重故障修复使用，事后必须补交完整审查表

### 使用紧急通道的条件
- [ ] ✅ 生产环境严重故障，必须立即修复
- [ ] ✅ 已通知团队成员
- [ ] ✅ 事后 48 小时内补交完整审查表

### 紧急通道标记
```yaml
emergency: true
emergency_reason: "[填写紧急原因]"
post_submit_deadline: "YYYY-MM-DD"
```

---

## 6. 审查批准（可选）

| 角色 | 姓名 | 审批意见 | 日期 |
|------|------|---------|------|
| 技术负责人 | | | |
| 架构师 | | | |
| 项目负责人 | | | |

---

## 7. 附加信息

### 相关文档
- [ ] TCB_SPEC.md
- [ ] REACTOR_SPEC.md
- [ ] GOVERNANCE_SPEC.md
- [ ] rule_taxonomy.md

### 关联 Issue
```
[在此列出关联的 Issue ID]
```

### 其他备注
```
[在此填写其他需要说明的信息]
```

---

## 附录：变更 ID 生成规则

```
格式: CR-YYYYMMDD-NNN
示例: CR-20260820-001

- YYYYMMDD: 变更提交日期
- NNN: 当日序号（从 001 开始）
```

## 附录：审查状态流转

```
待审查 → 已批准 → 实施
待审查 → 已拒绝 → 重新提交
紧急通过 → 补交审查表 → 已批准/已拒绝
```

---

> **注意**: 本模板是 evorule 变更治理的强制要求。
> 任何未按此模板填写的变更，其对应的构建将被自动拒绝。
