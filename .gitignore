# Rust 构建产物（统一到 .build/rust）
target/
**/target/
.build/

# 环境变量（可能含密钥）
.env
.env.*
!.env.example

# 数据文件
data/
*.db
*.sqlite
*.db-journal

# IDE
.vscode/
.idea/
*.swp
*.swo

# 操作系统
.DS_Store
Thumbs.db

# 临时文件
*.tmp
*.log
*.bak
*~

# Trae 临时文件
.trae/

# 门控日志目录（开发过程中的临时文件，不应 commit）
.gate-logs/

# === 2026-07-14 首次 git init 补充 ===

# Windows 构建产物 (probe.exe / probe.pdb 等)
*.exe
*.pdb

# 构建/安装日志 (evorule-tcb/install_log.txt 等)
install_log.txt
install_llvm_cov_log.txt

# Proptest 失败持久化文件 (掩盖 assertion bug 的元凶)
*.proptest-regressions

# Kani 形式化验证产物
kani-results/
*.kani
.kani-logs/

# 内部中文设计文档
# 物理位置: 仓外私有目录（NTFS junction）
# 防止 commit + 防误删 + 防误发布
_PRIVATE_zh_docs/
**/_PRIVATE_zh_docs/
**/_PRIVATE_zh_docs/**

# 仓内共享 L2/L3 设计/实现/基准文档（.gitignore 保护，不公开发布）
# 来源: AGENTS.md §内部约定: 文档/ 永不发布
文档/
**/文档/
**/文档/**

# === SDK 构建产物 (Python / TypeScript) ===
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/

# === SDK 已物理分离到独立仓 (v0.2.0+ 计划) ===
# 旧位置 sdk/go/ + sdk/java/ 已迁出
# sdk/go/
# sdk/java/

node_modules/
dist/
.tsbuildinfo
*.tsbuildinfo

# Python 临时测试脚本
sse_test_client.py

# === 2026-07-17 dev/wip WIP cleanup: 根目录 build/test output ===
# 不应该 commit 进 git history; 已在 commit 35c971b 中误加, 此次 amend 移除
/.a7_build.txt
/.check-output.txt
/.tier2_full.txt
/.tier2_test_output.txt

# === 2026-07-17 dev/wip 第二轮 cleanup: 用户重新生成的 build/test output ===
/build_out.txt
/test_out.txt

# === 2026-07-22 发布前盘点: 排除 benchmark / 一次性脚本产物 ===
docs/benchmarks/tmp/
docs/benchmarks/exp_*_output.txt
docs/benchmarks/exp_*_results.csv
docs/benchmarks/exp_*.txt
docs/benchmarks/exp_*_smoke.ps1
docs/benchmarks/sess*_audit.json
docs/benchmarks/server_help.txt
docs/benchmarks/*.log
docs/benchmarks/*_dump.json
find_p2.ps1

# === 2026-07-25 TLA+ 形式化验证 ===
# tla2tools.jar 由 CI 下载，不 commit 到仓库（2.2MB 二进制）
evorule-tcb/tla/tla2tools.jar
# TLC 模型检测的临时状态文件
evorule-tcb/tla/states/

# === 2026-07-30 发布前盘点: 排除 Kani/调试用临时 helper 脚本 ===
# 命名约定: __前缀 = 一次性分析脚本; _前缀 = 本地 WSL/调试辅助脚本
# 这些脚本不属于机制层发布内容,也不应进入长期 git 历史
scripts/__*.ps1
scripts/__*.sh
scripts/_*.ps1
scripts/_*.sh

# === 2026-08-10 发布前盘点: 根目录预编译二进制（musl 静态产物，应走 Release 资产而非 git history） ===
# 这两个文件是 evorule CLI 的 Linux musl 静态二进制，不应 commit 进 git
/evorule-aarch64
/evorule-x86_64
